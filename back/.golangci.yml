run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - gosimple
    - ineffassign
    - errcheck
    - gocritic
    - gofmt
    - goimports
issues:
  exclude-use-default: false
  max-same-issues: 0
  max-issues-per-linter: 0
