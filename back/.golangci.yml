version: "2"

run:
  timeout: 5m
  tests: true


linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - errcheck
    - gocritic

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-use-default: false
  max-same-issues: 0
  max-issues-per-linter: 0
