# Traefik Production Configuration with Let's Encrypt HTTPS

# API (disabled in production)
api:
  dashboard: false

# Entry Points
entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

# Certificate Resolvers (Let's Encrypt)
certificatesResolvers:
  letsencrypt:
    acme:
      email: "${TRAEFIK_CERTIFICATESRESOLVERS_LETSENCRYPT_ACME_EMAIL}"
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web

# Providers
providers:
  docker:
    exposedByDefault: false
    network: timemanager-network
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

# Logging
log:
  level: WARN

# Access Logs (optional, can be verbose)
accessLog:
  bufferingSize: 100
  filters:
    statusCodes:
      - "400-599"
